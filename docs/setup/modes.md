# modes

**Ask** — режим только для чтения: я разобрался по Makefile и docker-compose.yml, какие команды проверяют живость сервиса (`make up`/`make ps`/`make logs`, `curl /health`), но сам файл не правил и предложил переключиться.
**Code** — режим с правами на запись: в нём я тем же анализом дополнил README разделом «Как проверить, что сервис жив» и сохранил файл на диск.
Для разбора чужого кода (поиск багов, ревью, плановое чтение больших модулей) я бы взял explore-агента — он быстрее и дешевле, чем основной code-агент, и не трогает файлы.